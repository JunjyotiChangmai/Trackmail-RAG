services:
  trackmail-rag:
    build: .
    container_name: trackmail-rag-container
    ports:
      - "8000:8000"
    env_file:
      - .env
    restart: unless-stopped
    networks:
      - app-network

networks:
  app-network:
    driver: bridge